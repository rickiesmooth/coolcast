# import Show, User, PodcastPlay from "./generated/prisma.graphql"

type Query {
  show(id: ID!): Show
  me: User
}

type Mutation {
  getPodcast(showId: String!): Show!
  authenticate(facebookToken: String!): AuthPayload!
  addPlay(episodeId: ID!): PodcastPlay!
}

type AuthPayload {
  token: String!
  user: User!
}
